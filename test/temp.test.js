/* global describe, it */
'use strict';

var assert = require('assert');

describe('sse-channel', function() {
    it('works', function(done) {
        assert.equal('works', 'works', 'They should be the same');
        done();
    });
});